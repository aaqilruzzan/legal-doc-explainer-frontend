# API Configuration
VITE_API_BASE_URL=http://localhost:5001

# Note: Create a .env file with your actual values based on this template
# Make sure not to commit the actual .env file to version control
